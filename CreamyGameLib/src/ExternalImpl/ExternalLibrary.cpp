#include "ExternalImpl/ExternalLibrary.hpp"

/*
namespace creamyLib::internal
{
    impl::LibHandlePointer ExternalLib::Initialize(const impl::LibConfig& config)
    {
        impl::Initialize(config);
    }

    void ExternalLib::Finalize(const impl::LibHandlePointer libHandle)
    {
        impl::Finalize(libHandle);
    }
}
*/
